{
  "name": "org-deletion",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/org-deletion/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:org-deletion"],
  "// targets": "to see all targets run: nx show project org-deletion --web",
  "targets": {
    "build": {
      "executor": "nx:noop",
      "outputs": [],
      "dependsOn": []
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/org-deletion"],
      "options": {
        "jestConfig": "libs/org-deletion/jest.config.cts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project libs/org-deletion/tsconfig.json --noEmit"
      }
    }
  }
}
