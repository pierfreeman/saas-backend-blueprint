{
  "name": "admin-memberships",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/admin/memberships/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:admin"],
  "// targets": "to see all targets run: nx show project admin-memberships --web",
  "targets": {
    "build": {
      "executor": "nx:noop",
      "outputs": [],
      "dependsOn": []
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/unit/libs/admin/memberships"],
      "options": {
        "configFile": "libs/admin/memberships/vitest.config.ts",
        "passWithNoTests": true
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project libs/admin/memberships/tsconfig.json --noEmit"
      }
    }
  }
}
