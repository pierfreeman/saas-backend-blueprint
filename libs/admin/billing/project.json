{
  "name": "admin-billing",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/admin/billing/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:admin"],
  "// targets": "to see all targets run: nx show project admin-billing --web",
  "targets": {
    "build": {
      "executor": "nx:noop",
      "outputs": [],
      "dependsOn": []
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/unit/libs/admin/billing"],
      "options": {
        "configFile": "libs/admin/billing/vitest.config.ts",
        "passWithNoTests": true
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project libs/admin/billing/tsconfig.json --noEmit"
      }
    }
  }
}
