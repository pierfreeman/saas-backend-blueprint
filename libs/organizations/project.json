{
  "name": "organizations",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/organizations/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:organizations"],
  "targets": {
    "build": {
      "executor": "nx:noop",
      "outputs": [],
      "dependsOn": []
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/organizations"],
      "options": {
        "jestConfig": "libs/organizations/jest.config.cts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project libs/organizations/tsconfig.json --noEmit"
      }
    }
  }
}
