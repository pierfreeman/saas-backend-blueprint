/**
 * OrganizationsService unit tests live in the library:
 * libs/organizations/src/organizations.service.spec.ts
 *
 * The app-layer service is a pure re-export and carries no logic of its own.
 */
it('is a re-export — see libs/organizations/src/organizations.service.spec.ts', () => {
  expect(true).toBe(true);
});
