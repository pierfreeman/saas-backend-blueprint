/**
 * MembershipsService unit tests live in the library:
 * libs/memberships/src/memberships.service.spec.ts
 *
 * The app-layer service is a pure re-export and carries no logic of its own.
 */
it('is a re-export — see libs/memberships/src/memberships.service.spec.ts', () => {
  expect(true).toBe(true);
});
